PORT=5000
NODE_ENV=development

# Comma-separated browser origins allowed by CORS (your Vercel app + locals if needed)
CLIENT_ORIGINS=https://shoe-testing-frontend.vercel.app

# Add your DB, JWT_SECRET, etc. alongside these.
